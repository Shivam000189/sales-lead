# Phase 1: Plan 01 - Foundations & Core Bug Fixes

## Goal
Harden security, fix runtime authentication and session handling defects, complete the lead status pipeline, establish structured activity types, and provide a team members API endpoint.

---

## Tasks

### Task 1: Sanitize Plaintext Production Credentials
- **File**: `DEPLOYMENT.md`
- **Action**:
  - Replace lines 24-25 in `DEPLOYMENT.md` with environment variable placeholders (`MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/?appName=Cluster0` and `JWT_SECRET=your-production-jwt-secret`).
- **Verification**: Ensure no active secrets or credentials remain in documentation.

---

### Task 2: Fix Authentication Profile Retrieval (`/api/auth/me`)
- **File**: `server/src/controllers/authController.js`
- **Action**:
  - Line 80: Replace `getMe(req.userId)` with `getMe(req.user.id)`.
  - Lines 91-99: Return clean user profile without referencing nonexistent `googleId`.
- **Verification**: `GET /api/auth/me` with a valid Bearer token returns 200 with the user object (`_id`, `name`, `email`, `role`).

---

### Task 3: Prevent 403 Forbidden Responses from Wiping User Sessions
- **File**: `client/src/api/axios.js`
- **Action**:
  - In response interceptor, change condition `if (error.response?.status === 401 || error.response?.status === 403)` to check only `401`.
  - On `403`, allow the rejection to propagate so the UI can display unauthorized notifications without logging the user out.
- **Verification**: Triggering an unauthorized action (e.g. member attempting an admin action) does not redirect to `/login` or clear `localStorage`.

---

### Task 4: Complete Pipeline Metrics & Dashboard Display
- **Files**:
  - `server/src/services/dashboardService.js`
  - `client/src/pages/CrmPages.jsx`
- **Action**:
  - In `dashboardService.js`: Add `Lead.countDocuments({ status: "PROPOSAL_SENT" })` to the aggregation `Promise.all` array.
  - Return `proposalSent: proposalSentLeads` in stats response.
  - In `client/src/pages/CrmPages.jsx`: Add `["Proposal sent", "proposalSent", "✉", "cyan"]` to the `stats` array in `Dashboard` component.
- **Verification**: Dashboard renders 7 metric cards including `Proposal sent`.

---

### Task 5: Enrich Activity Schema with Structured Action Types
- **Files**:
  - `server/src/models/Activity.js`
  - `server/src/services/activityService.js`
- **Action**:
  - In `Activity.js`: Add `type` field with enum `["STATUS_CHANGE", "NOTE_ADDED", "EMAIL_SENT", "ASSIGNED", "LEAD_CREATED", "LEAD_DELETED"]` and default `"STATUS_CHANGE"`.
  - In `activityService.js`: Update `createActivity(leadId, action, userId, type)` to persist `type`.
- **Verification**: Newly generated activity records contain both `action` string and `type` enum.

---

### Task 6: Implement Team Members Query API (`GET /api/users`)
- **Files to Create / Modify**:
  - `server/src/controllers/userController.js` (NEW)
  - `server/src/routes/userRoutes.js` (NEW)
  - `server/src/index.js`
- **Action**:
  - Implement `userController.js:getUsers` querying `User.find().select("-password").sort({ name: 1 })`.
  - Define `router.get("/", authenticate, getUsers)`.
  - Mount router at `/api/users` in `server/src/index.js`.
- **Verification**: `GET /api/users` with Bearer token returns list of team members with id, name, email, and role.

---

### Task 7: Fix Schema Naming Mismatch & Lead Deletion Audit
- **Files**:
  - `server/src/validations/leadValidation.js`
  - `server/src/routes/leadRoutes.js`
  - `server/src/controllers/leadController.js`
- **Action**:
  - In `leadValidation.js`: Add dedicated `updateStatusSchema = z.object({ status: z.enum(leadStatus) })` and rename general schema to `updateLeadSchema`.
  - In `leadRoutes.js`: Apply `validate(updateLeadSchema)` to `PATCH /api/leads/:id` and `validate(updateStatusSchema)` to `PATCH /api/leads/:id/status`.
  - In `leadController.js:remove`: Pass `req.user.id` to `deleteLead(req.params.id, req.user.id)`.
- **Verification**: Status patch route enforces enum; lead deletion records `performedBy` properly.

---

## Verification Checklist
- [ ] No real secrets exist in `DEPLOYMENT.md`.
- [ ] `GET /api/auth/me` resolves the active user from `req.user.id`.
- [ ] 403 response does not purge credentials or redirect.
- [ ] Dashboard displays `Proposal sent` count.
- [ ] Activity documents include `type` field.
- [ ] `GET /api/users` returns team member list.
- [ ] Input validation correctly guards general vs status updates.
